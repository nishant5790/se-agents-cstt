# Core (always needed)
pydantic>=2.6
python-dotenv>=1.0
python-pptx>=0.6.23
certifi>=2024.2

# Demo web API + UI
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Optional per-format extractors (install what you need)
openpyxl>=3.1          # .xlsx / .xls
pypdf>=4.0             # .pdf
vosk>=0.3.45           # audio/video transcription (offline)
imageio-ffmpeg>=0.4.9  # audio extraction from video
numpy>=1.26            # frame similarity math (video analyser)
Pillow>=10.0           # frame loading/resizing (video analyser)

# Optional LLM path (Azure OpenAI). Without it the team runs deterministically.
openai>=1.40

# Optional Azure media extraction (transcript + frame OCR). See azure_media_extractor.py
azure-ai-documentintelligence>=1.0  # slide/frame OCR (optional, only if AZURE_DOCINTEL_* set)
